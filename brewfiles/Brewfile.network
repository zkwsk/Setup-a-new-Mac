# VPN and tunnel clients.
# @attended Approve the VPN system extensions in System Settings > Network after install.
brew "wireguard-tools"
cask "tailscale-app"
cask "viscosity"
cask "private-internet-access"
